# The port the server will run on
PORT=5000

# Connection string for your MongoDB database (where all user and app data is saved)
MONGODB_URI=

# A secure, random string used to encrypt/sign user login tokens (prevents fake logins)
JWT_SECRET=

# Your Google Gemini API key to access AI features
GEMINI_API_KEY=
